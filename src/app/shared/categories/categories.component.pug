.categories('*ngIf'='categories')
 ul
  li(*ngFor="let category of categories")
   a('routerLink'="" '[queryParams]'="{ category: category}" '[queryParamsHandling]'= "merge" 'routerLinkActive'="active")  {{category}} 