import { HoverGrowDirective } from './hover-grow.directive';

describe('HoverGrowDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverGrowDirective();
    expect(directive).toBeTruthy();
  });
});
