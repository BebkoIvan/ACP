.quizCard('*ngIf'='quiz')
 .quizCard__header
  .quizName
   span('(click)' = 'navigateTo()') {{quiz.name}}
  button.deleteQuiz('*ngIf'='isEditable' '(click)'='deleteQuiz($event)')
   i.icon-bin  
 .card-footer
  app-timestamp([date]="quiz.createdAt", [format]="'fullDate'") 
  span {{author$ | async}}
