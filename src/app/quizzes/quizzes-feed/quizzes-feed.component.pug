app-viewport
 .mainContent('*ngIf'="quizzes1 | async ")
  .quizCard('*ngFor'='let quiz of quizzes')
   .quizName
    a(routerLink="../{{quiz.id}}" )
     span {{quiz.name}}
   .card-footer
    app-timestamp([date]="quiz.createdAt",[format]="'fullDate'") 
    span {{quiz.author}}
    button('(click)'='deleteQuiz(quiz)')
     i.icon-cross
 app-auxiliary-content([auxContActive]="acaActive",(acaClick)="acaHandler()") 
  h2 Create your own quiz!    
