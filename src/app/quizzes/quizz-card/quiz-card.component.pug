.quizCard('*ngIf'='quiz')
   .quizName
    a(routerLink="../{{quiz.id}}" )
     span {{quiz.name}}
   .card-footer
    app-timestamp([date]="quiz.createdAt", [format]="'fullDate'") 
    span {{author$ | async}}
    button('(click)'='deleteQuiz($event)')
     i.icon-bin