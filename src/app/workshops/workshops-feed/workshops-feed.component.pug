.workshops((scroll)="onScroll($event)")
 app-article(*ngFor="let article of articles",[article]="article",(scrollTop)="scrollTop()")
 app-to-top-button([scrollPosition]="scrollTop1")
app-auxiliary-content([auxContActive]="acaActive",(acaClick)="acaHandler()")
 
